type Query {
    studentById(id: ID): Student
}

type Mutation {
    addStudent(input: StudentInput): Student
}

type Student {
    id: ID
    name: String
    secondName: String
    grade: Int
}

input StudentInput {
    name: String!
    secondName: String!
    grade: Int
}